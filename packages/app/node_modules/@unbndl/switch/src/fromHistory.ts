import { fromService } from "@unbndl/service";
import * as History from "./history.ts";

export function fromHistory(
  target: HTMLElement,
  contextLabel: string = History.CONTEXT_DEFAULT
) {
  return fromService<History.Model>(target, contextLabel);
}
