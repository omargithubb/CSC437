import { Context } from '@unbndl/html';
import { Provider } from './provider.ts';
import { Update } from './update.ts';
import * as Message from "./message.ts";
type None = Message.None;
export declare class Service<Msg extends Message.Base, T extends object> {
    _context: Context<T>;
    _update: Update<Msg, T>;
    _eventType: string;
    _running: boolean;
    _pending: Array<Msg>;
    constructor(update: Update<Msg, T>, context: Context<T>, eventType?: string, autostart?: boolean);
    attach(host: Provider<T>): void;
    start(): void;
    consume(message: Msg | None): void;
    process(message: Msg): void;
}
export {};
